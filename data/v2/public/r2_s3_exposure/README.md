# R2-S3 Exposure-Aware Ablation Evidence

Package: `r2_s3_exposure`

Private source run: `r2-s3-dev-exposure-20260721-01`

Private manifest SHA-256: `f7e519beb0c9e054b5de452348d214b2a39a4bec3979302063fdd2475cd6b0d6`

Decision: `NO_CURRENT_BYPASS_OBSERVED`

This content-free package contains fingerprinted attack-unit rows and bounded aggregate witnesses. Run `python verify.py` to validate every artifact, recompute metrics and strata, and reapply decision precedence.

The checksums detect corruption. Compare the declared private manifest SHA-256 with an externally trusted value, then re-export from that trusted private run to verify projection provenance; this isolated package alone does not prove that derivation.

Authenticate `verify.py` against a trusted copy before relying on isolated verification; package-internal hashes cannot authenticate verifier bytes.

This dev-only deterministic replay does not establish universal runtime safety. Counterfactual coverage is diagnostic, does not measure wall-clock latency, and does not admit a production retrieval change.
