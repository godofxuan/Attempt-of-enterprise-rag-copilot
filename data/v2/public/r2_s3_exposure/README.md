# R2-S3 Exposure-Aware Ablation Evidence

Package: `r2_s3_exposure`

Private source run: `r2-s3-dev-exposure-20260721-02`

Private manifest SHA-256: `0c2e074d5b8ba2c4396691a58f1d81cc802d5feb1c200f2eccf661f11d5f0585`

Decision: `NO_CURRENT_BYPASS_OBSERVED`

This content-free package contains fingerprinted attack-unit rows and bounded aggregate witnesses. Run `python verify.py` to validate every artifact, recompute metrics and strata, and reapply decision precedence.

The checksums bind package bytes. Compare the declared private manifest SHA-256 with an externally trusted manifest record, then re-export from that trusted private run to verify projection provenance; this isolated package alone does not prove that derivation.

Compare `verify.py` bytes with an independently trusted copy before relying on isolated verification; package-internal hashes cannot establish trust in verifier bytes.

This dev-only deterministic replay does not establish universal runtime safety. Counterfactual coverage is diagnostic, does not measure wall-clock latency, and does not admit a production retrieval change.
