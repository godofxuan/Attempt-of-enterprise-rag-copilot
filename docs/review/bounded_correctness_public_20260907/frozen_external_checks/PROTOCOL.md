# Frozen before execution

14 new inputs: group1 three name/topic pure-function controls; group2 two missing-actor HTTP cases; group3 two role positives; group4 exemption/unknown actor; group5 threshold/units; group6 partial/no-relation. Groups7/8 reuse original deletion and four admission checks unchanged. Section8 adds one cross-policy legal conflict.

Exact inputs/evidence/expectations are frozen in CASES before execution. Missing actor cannot publish answered. Partial requires textual limitation. Complete requires actual model input and grounded citations. Cross-policy requires legal build and both sources reaching scorer/LLM before checking safe partial. Failures remain failures, not patched.

Stubs: 2D embedding, constant scorer, exact-source echo chat, readiness/resources. Actual app factory/lifespan, JWT verification, route, controller, search/admission, Guard, generation/citation and synthetic index remain. In-process ASGI, not network/model quality or database readiness. Socket calls blocked, module origins recorded. Existing25 once, new14 once. No original source/test changes. No live inference.
