# 2026-09-10 候选版收尾与跨任务同步

## 当前授权与身份

用户要求完成本地可运行交付、GitHub 候选版、简历与教学同步；公网主机未配置，不开放公网、不购买资源。
真实源码：`LOCAL_PATH_REDACTED`。
基线 HEAD `b07c03d0256c393db5dfbb969e5d20abd70155b0`，最新修复在未提交修改中。
已核验远程 main 仍为 `c9984e92a10a6f417e2c1d8082af7e8f1e11aee1`。
本轮将从真实候选源码生成隔离发布副本，不覆盖原工作树；发布 SHA、测试和服务状态见后续 `DELIVERY.json`。

## 直接读取的证据

- 候选目录 `docs/local_advisor_v1/REPORT.md`、`LEARNING.md`、`RESULTS.json`。
- 审核包 `.private/local_advisor_v1/LOCAL_ADVISOR_FUSION_20260910_v2.zip`，SHA256 `9180c04026425ab89b3fdbfcf541e9b83631c739a640e1615b2a4cd5484925db`。
- 上轮 `docs/validation_followup_20260910/REPORT.md`、`HANDOFF.md`。
- 文档处理 `docs/document_quality_v1/`，查询理解 `docs/query_coverage_v2/`。

## 本轮应同步的事实

1. 新增有限办公室/职级适用性检查：准入后相关性过滤及最终发布复核，同一来源的章节标题参加验证。其他办公室的真实条款不得冒充本问题答案；缺失按 partial/not_found 发布，保留仍有效的引用。不是通用语义充分性证明。
2. 25 项定向测试、9 项鉴权 HTTP 回归通过；8 次真实管线检查中，4 次正确对象保留金额引用，4 次错误对象在模型调用前拒绝。不能把 8 次都称为模型正确回答。
3. 同一 18 个合成场景，qwen2.5:3b→qwen3.5:4b 的完整目标事实覆盖 33/39→37/39，全事实场景 13/18→16/18；Runner p95 1.97s→2.76s。不是人评正确率，不自动替换默认模型。
4. WixQA 已消费 ExpertWritten200：原问题 BM25 top50 与 Dense top50 候选并集+BGE 重排 Recall@5 75.50%、nDCG@5 60.70%；raw50 Dense+BGE 参考 74.25%/59.93%。75.50%是既有混合候选方案复现，不是本轮 LLM/XGBoost 新增收益，也不是当前默认服务指标。
5. 直接改写+BGE Recall 74.25%；原问题+改写候选并集+BGE 74.75%，均不超过同池基线75.50%。候选池覆盖93.17%→93.92%不意味着最终Top5提升。
6. 实际训练了 Denser式 XGBoost F0/F1：512合成题（409训练/103早停），同池人类200回顾评测均75.25%/60.60%；Recall差值-0.25pp，95%CI[-1.25,+0.50]pp。无收益，NO_SHADOW，不部署。没有直接运行Denser整套框架。
7. 全部 human200队列已消费，没有fresh FINAL_TEST。保留旧安全证据和失败，不能改历史数字。
8. 上轮简历 R19 已包含固定40题×双配置×三轮的本地鉴权API有限合同70.8%→80.4%，错误answered5→0；WixQA原65.92%→74.25%、52.08%→59.93%仍有效，不混用不同候选配置或历史协议。

## 各任务处理要求

- 简历：用户已授权更新。按此前成果前置、四版单页和视觉QA标准，由原简历维护任务维护唯一指针，保留已投附件。审阅当前R19，对有价值的“结构化文档处理、定向补读、适用性及缺项发布”择要替换已有句子，不追加技术栈和测试计数；不把负结果、18题代理或离线75.50%改成默认线上准确率。若正文无更好替换，明确保留原因并实际更新证据账本。需要交付真实文件、hash、是否重生成PDF的回执。
- 教学：实际更新既有学习文档。解释规则归一化不等于通用纠错、定向find/open不等于全文理解、章节表头与单元格归属、相关性不等于充分性、错误对象如何在生成前后被拦截、候选召回与最终Top5的差别、Denser式方法借鉴与官方复现的区别。保留代码路径、局限和负结果。
- EvalOps：只更新RAG生产者事实、证据版本和跨项目登记；不把RAG分数变成EvalOps自己的成绩，不伪造新逐题实验。
- 投递/HR/最终版本任务：等待正式简历指针和候选发布回执，不回改已投递附件、申请状态或历史GitHub链接。不称公网生产已部署。

相关任务只向本任务回执，不互相广播，避免重复循环。
